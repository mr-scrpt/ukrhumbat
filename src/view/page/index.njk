{% set inject = { layoutClass: "page page_theme layout deco"} %}
{% extends "layout/layout.njk" %}
{%- from "layout/layout-section-macros.njk" import layout_base_section -%}
{%- from "layout/layout-footer-macros.njk" import layout_base_footer -%}
{% block main %}
    {#  About seciton #}
    {{ layout_base_section({
    component: 'about',
    titleText: 'Про нас',
    layoutClass: 'layout__wrap',
    sectionClass: '',
    sectionInnerClass:'deco__bg-alt'
    })
    }}
    {# About seciton END #}
    {# News seciton #}
    {{ layout_base_section({
    component: 'news',
    titleText: 'Новости',
    layoutClass: 'layout__wrap deco__text-shadow',
    sectionClass: '',
    sectionInnerClass:'',
    sectionDecoText: 'Новости',
    news_list: news.news_list
    })
    }}
    {# News seciton END #}
    {# Result seciton #}
    {{ layout_base_section({
    component: 'result',
    titleText: 'Наши результаты',
    layoutClass: 'layout__wrap',
    sectionClass: '',
    sectionInnerClass:'deco__bg-alt',
    result_list: result.result_list
    })
    }}
    {# Result seciton END #}
    {# Result seciton #}
    {{ layout_base_section({
    component: 'support',
    titleText: 'напрямки підтримки',
    layoutClass: 'layout__wrap deco__text-shadow deco__text-shadow_rev',
    sectionClass: '',
    sectionInnerClass:'',
    sectionDecoText: 'Підтримка',
    support_list: support.support_list
    })
    }}
    {# Result seciton END #}
    {# Donat seciton #}
    {{ layout_base_section({
    component: 'donat',
    layoutClass: 'layout__wrap',
    sectionClass: '',
    sectionInnerClass:'deco__bg-alt'
    })
    }}
    {# Donat seciton END #}
    {# Team seciton #}
    {{ layout_base_section({
    component: 'team',
    titleText: 'Команда',
    layoutClass: 'layout__wrap deco__text-shadow',
    sectionClass: '',
    sectionInnerClass:'',
    sectionDecoText: 'Команда',
    team_list: team.team_list
    })
    }}
    {# Team seciton END #}
    {# Contact seciton #}
    {{ layout_base_section({
    component: 'contact',
    titleText: 'Контакти',
    layoutClass: 'layout__wrap',
    sectionClass: '',
    sectionInnerClass:'deco__bg-alt',
    contact_list: contact.contact_list
    })
    }}
    {# Contact seciton END #}
    {# Form contact seciton #}
    {{ layout_base_section({
    component: 'form-contact',
    layoutClass: 'layout__wrap',
    sectionClass: 'layout__section_free',
    sectionInnerClass:'section__inner_only_bottom section__inner_last deco__bg-alt'
    })
    }}
    {# Form contact seciton END #}
    {# Footer contact seciton #}
    {{ layout_base_footer({
    component: 'footer',
    layoutClass: 'layout__wrap',
    sectionClass: 'layout__section_free',
    sectionInnerClass:'deco__bg-gradient-v section__inner_footer',
    menu_list: menu.menu_list,
    contact_list: contact.contact_list
    })
    }}
    {# Footer contact seciton END #}
{% endblock main %}
